from setuptools import setup, find_packages

setup(
    name='lsm',
    version='0.1',
    packages=find_packages(),
#    install_requires=['numpy >= 1.23.4', 'scipy >= 1.6.0'],
    zip_safe=False,
)
